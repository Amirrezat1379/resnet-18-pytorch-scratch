import torch
import torch.nn as nn

class residualBox(nn.Module):
    def __init__(self, in_channels, channels, stride = 1, last = False):
        super().__init__(residualBox)
        self.last = last
        